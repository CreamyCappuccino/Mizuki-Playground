name: Earth Axial Tilt CI
on:
  push:
    branches: [main]
    paths:
      - 'earth-axial-tilt/**'
      - '.github/workflows/earth-axial-tilt.yml'
  pull_request:
    paths:
      - 'earth-axial-tilt/**'
      - '.github/workflows/earth-axial-tilt.yml'
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: earth-axial-tilt-${{ github.ref }}
  cancel-in-progress: true
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: earth-axial-tilt
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: earth-axial-tilt/package-lock.json
      - name: Reproducible install
        run: npm ci
      - name: Document and translation checks
        run: npm run check:docs
      - name: Typecheck
        run: npm run typecheck
      - name: Unit tests
        run: npm test
      - name: Production build
        run: npm run build
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Chromium regression suite
        run: npm run test:e2e
      - name: Browser evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: earth-chromium-${{ github.run_number }}
          path: earth-axial-tilt/test-results/
          if-no-files-found: ignore
          retention-days: 7
  webkit:
    runs-on: macos-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: earth-axial-tilt
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: earth-axial-tilt/package-lock.json
      - run: npm ci
      - run: npm run build
      - name: Install WebKit
        run: npx playwright install webkit
      - name: macOS WebKit regression (not on-device iPhone Safari)
        run: npm run test:e2e:webkit -- e2e/v06.spec.ts e2e/v07.spec.ts e2e/v08.spec.ts e2e/v09.spec.ts e2e/v10.spec.ts --workers=2
      - name: WebKit evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: earth-webkit-${{ github.run_number }}
          path: earth-axial-tilt/test-results/
          if-no-files-found: ignore
          retention-days: 7
